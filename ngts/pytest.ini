[pytest]
markers =
    ngts_skip
    build
    push_gate
    vlan
    fdb
    lldp
    dhcp_relay

log_cli = True
log_format = %(asctime)s [%(levelname)8s] (%(filename)s:%(lineno)s) %(message)s
log_date_format=%Y-%m-%d %H:%M:%S
